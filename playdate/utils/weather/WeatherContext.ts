// 'use client'

// import { createContext, useContext } from "react";

// export interface WeatherData {
//     latitude: number;
//     longitude: number;
//     error?: string;
//     weather_id?: number;
//     weather_icon?: number;
//     current_temp?: number;
//     low_temp?: number;
//     high_temp?: number;
// }
// // Initialize new context for location
// export const WeatherContext = createContext<WeatherData>({
//     latitude: 0,
//     longitude: 0    
// });

// // We create a custom hook to provide immediate usage of the location context in other components
// export const useLocationContext = () => useContext(WeatherContext);

